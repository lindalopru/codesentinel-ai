[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers --strict-config
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    integration: requires a running Ollama daemon and a pulled model
    slow: takes more than ~1s
